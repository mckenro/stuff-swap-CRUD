export class Listing {
  constructor (public title: string, public description: string, public price: number, public city: string, public state: string, public zip: number, public condition: string, public image: string) { }
}
